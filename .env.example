# Copy this file to ".env" and fill in real values.
# The real ".env" is gitignored and must NEVER be committed.
#
# Generate fresh values with:
#   python3 -c "import secrets; print(secrets.token_hex(16))"   # for POSTGRES_PASSWORD
#   python3 -c "import secrets; print(secrets.token_hex(32))"   # for APP_SECRET

POSTGRES_PASSWORD=changeme_use_a_real_random_value
APP_SECRET=changeme_use_a_real_random_value
